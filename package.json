{
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "cypress": "^15.12.0"
  },
  "dependencies": {
    "-": "^0.0.1",
    "cypress-ajv-schema-validator": "^2.0.2",
    "save-dev": "^0.0.1-security"
  },
  "scripts": {
    "test:open": "cypress open",
    "test:all": "cypress run",
    "test:api": "cypress run --spec \"cypress/e2e/api/**\" --browser chrome",
    "test:ui": "cypress run --spec \"cypress/e2e/ui/**\" --browser chrome"
  }
}
